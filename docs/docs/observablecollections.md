---
hide:
  - toc
---

# ObservableCollection classes
These are pretty much the same as the original counterpart collections. And the you cannot use a normal `ChangeListener` and must instead use the specific change listeners provided. These extend from the parent interfaces to allow you to use them without having to replace existing field/variable types.  
Note: Some operations are not supported and throw an `UnsupportedOperationException`. 