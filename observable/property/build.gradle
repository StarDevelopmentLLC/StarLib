plugins {
    id 'java-library'
    id 'maven-publish'
}

apply from: rootProject.file('buildSrc/shared.gradle')

repositories {
    mavenCentral()
}

jar {
    archiveBaseName.set(rootProject.name + '-observable-property')
    destinationDirectory.set(file("$rootProject.projectDir/build/libs"))
}

publishing {
    publications {
        maven(MavenPublication) {
            from components.java
            artifactId = 'observable-property'
        }
    }
}

dependencies {
    api project(':observable:writable')
    api project(':observable:constants')
}